class CardService:
    # TODO: [Member 3] Implement CRUD operations for Cards.
    # Requirements:
    # 1. create_card(user_id, question, answer)
    # 2. get_cards_by_user(user_id)
    # 3. get_card_by_id(card_id)
    # 4. update_card(card_id, data)
    # 5. delete_card(card_id)
    pass
